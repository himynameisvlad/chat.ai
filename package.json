{
  "name": "chat-ai",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --workspace=chat-backend\" \"npm run dev --workspace=frontend\"",
    "dev:backend": "npm run dev --workspace=chat-backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspaces",
    "build:shared": "npm run build --workspace=@chat-ai/shared",
    "build:backend": "npm run build --workspace=@chat-ai/shared && npm run build --workspace=chat-backend",
    "build:frontend": "npm run build --workspace=@chat-ai/shared && npm run build --workspace=frontend",
    "review:pr": "tsx scripts/review-pr.ts"
  },
  "devDependencies": {
    "@octokit/rest": "^20.0.2",
    "concurrently": "^9.2.1",
    "tsx": "^4.7.0"
  }
}
